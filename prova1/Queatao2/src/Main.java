/**
 * Herança:
 * Herança é um dos pilares da Programação Orientada a
 * Objetos e permite que uma nova classe (chamada de
 * subclasse ou classe derivada) herde propriedades e
 * comportamentos de uma classe existente (chamada de
 * superclasse ou classe base).
 *
 * Exemplo prático:
 * No contexto do sistema de gerenciamento de
 * bibliotecas, podemos ter uma classe genérica chamada
 * ItemDeBiblioteca que possui atributos
 * como ID, titulo e dataDeAquisição.
 *
 * A partir desta classe, podemos derivar classes
 * específicas como Livro, Revista e DVD.
 * A classe Livro pode adicionar atributos específicos
 * como autor e ISBN, enquanto a classe DVD pode ter
 * atributos como diretor e duração.
 *
 */
public class Main {
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }
}